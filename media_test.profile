<?php

/**
 * Implements hook_install_tasks().
 *
 * Add additional tasks to the install profile install process.
 */
function media_test_install_tasks($install_state) {
  $tasks = array();

  return $tasks;
}
